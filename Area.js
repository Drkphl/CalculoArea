const prompt = require('prompt-sync')({ sigint: true });
const Func = require('CalculoArea/Funcoes.js');

let forma = prompt('qual forma geometrica iremos calcular a área?');

switch (forma) {
    case 'Circulo':
        break;
    case 'Retangulo':
        break;
    case 'Retangulo':
        break;
    default:
        break;
}
